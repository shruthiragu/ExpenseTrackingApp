<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Expense.Models"
             x:Class="Expense.Views.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="ExpenseListView"
                      
                      Margin="20"
                      ItemSelected="ExpenseListView_ItemSelected"></ListView>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:BudgetExpense">
                    var category="{Binding ExpenseCategory}"
                    <ImageCell
                        ImageSource={($"Assets/{category}.png")}
                        Text="{Binding Amount}" 
                        Detail="{Binding Name}"/>
                   
                </DataTemplate>
            </ListView.ItemTemplate>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
    