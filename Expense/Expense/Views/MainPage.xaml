<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:model="clr-namespace:Expense.Models"
             x:Class="Expense.Views.MainPage">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="ExpenseListView"
                      
                      Margin="20"
                      ItemSelected="ExpenseListView_ItemSelected"></ListView>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="model:BudgetExpense">
                    <ImageCell
                         Text="{Binding Amount}" Detail="{Binding Text}"/>
                   
                </DataTemplate>
            </ListView.ItemTemplate>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
    